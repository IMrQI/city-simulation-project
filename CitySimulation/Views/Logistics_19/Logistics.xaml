<UserControl x:Class="CitySimulation.Views.Logistics_19.LogisticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CitySimulation.Views.Logistics_19"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Форма ввода -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <Label Content="Создание заказа" FontSize="16" FontWeight="Bold"/>
            <Label Content="Компания" FontSize="14"/>
            <TextBox x:Name="TxtCompanyName" Margin="0,5"/>
            <Label Content="Адрес сбора" FontSize="14"/>
            <TextBox x:Name="TxtPickupAddress" Margin="0,5"/>
            <Label Content="Адрес доставки" FontSize="14"/>
            <TextBox x:Name="TxtDeliveryAddress" Margin="0,5"/>
            <Label Content="Номер машины" FontSize="14"/>
            <TextBox x:Name="TxtVehicle" Margin="0,5"/>
            <Label Content="Фио перевозщика" FontSize="14"/>
            <TextBox x:Name="TxtDriverName" Margin="0,5"/>
            <Button Content="Добавить заказ" Margin="0,10" Click="BtnAddOrder_Click"/>
        </StackPanel>

        <!-- Кнопки сохранения и загрузки -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Сохранить отчет" Margin="0,0,10,0" Click="BtnSaveReport_Click"/>
            <Button Content="Загрузить отчет" Click="BtnLoadReport_Click"/>
        </StackPanel>

        <!-- Список заказов -->
        <DataGrid Grid.Row="2" x:Name="OrdersGrid" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Компания" Binding="{Binding CompanyName}" Width="*"/>
                <DataGridTextColumn Header="Забор" Binding="{Binding PickupAddress}" Width="*"/>
                <DataGridTextColumn Header="Доставка" Binding="{Binding DeliveryAddress}" Width="*"/>
                <DataGridTextColumn Header="Машина" Binding="{Binding Vehicle}" Width="*"/>
                <DataGridTextColumn Header="Перевозчик" Binding="{Binding DriverName}" Width="*"/>
                <DataGridTextColumn Header="Дата" Binding="{Binding DateCreated, StringFormat=yyyy-MM-dd HH:mm}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>